import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Country Club Resorts Balakot - Luxury Mountain Retreat',
  description: 'Discover luxury and nature at Country Club Resorts Balakot with riverside cottages, hand-laid stone architecture, and exclusive experiences.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
